---
description: Show the Terminals ontology — one atom, many projections. The constructed objects, the ladder, and the ontological functions.
argument-hint: "[optional: a section (atom|ladder|functions|objects) or a named object/function]"
allowed-tools: mcp__terminals__ontology, Read
---
<!-- SPDX-License-Identifier: CC-BY-4.0 -->
<!-- Copyright (c) 2026 Tej Desai / Intuition Labs LLC -->

Read the Terminals ontology via `mcp__terminals__ontology` (pass `$ARGUMENTS` as `query` when
given — a section like `atom`/`ladder`/`functions`/`objects`, or a named object/function like
`Recipe`/`leap`). Then explain it plainly: the one externally-gated interaction-arrow weighted by
coherence R, the nine constructed objects (★ Recipe/Firewall/Kind are net-new; the rest credit
gebrüder/wirken), the isoformer ladder, and the ontological functions. If the user named a thing
(a listing, a recipe, a film, a trade), map it onto the atom and the ladder rung it sits on.
Field reading, not advice; the math is real, the edge unproven. Canonical site: intuitionlabs.tech/ontology.
